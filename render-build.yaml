# Render Build Configuration for Native Python
# https://render.com/docs/deploy-python

# Build command
build: pip install -r requirements.txt

# Start command  
start: cd backend_python && uvicorn main:app --host 0.0.0.0 --port $PORT
