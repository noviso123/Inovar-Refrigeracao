{
    "name": "inovar-refrigeracao",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend-svelte && npm install && npm run build && mkdir -p ../api/static && cp -r build/* ../api/static/"
    }
}